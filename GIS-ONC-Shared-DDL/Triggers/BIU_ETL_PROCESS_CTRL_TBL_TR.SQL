CREATE OR REPLACE
TRIGGER BIU_ETL_PROCESS_CTRL_TBL_TR 
BEFORE INSERT OR UPDATE ON ETL_PROCESS_CONTROL 

FOR EACH ROW 
BEGIN 
  IF UPDATING THEN
   
	SELECT SYSDATE INTO :NEW.AUD_MOD_DT FROM dual;
	SELECT USER INTO :NEW.AUD_MOD_USR_ID FROM dual;
END IF;
END;