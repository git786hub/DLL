CREATE OR REPLACE
TRIGGER GIS_ONC.BIU_CULIB_UNITATTRIBUTE_TR 
BEFORE UPDATE ON GIS_ONC.CULIB_UNITATTRIBUTE

FOR EACH ROW 
BEGIN 
  IF UPDATING THEN
   
    SELECT SYSDATE INTO :NEW.AUD_CREATE_DT FROM dual;
		SELECT USER INTO :NEW.AUD_CREATE_USR_ID FROM dual;
  END IF;

END;
/